<div class="text-center">

    <br>

    <br>

    <img id="burgerPic" src="https://i.imgur.com/meCuTz1.jpg" style="height:300px; width:450px;">

</div>
<br>

<div class="container">
    <div class="row">

        <div class="col-xs-12 col-md-4">
            <h2 class="text-center">Burgers on the Menu</h2>
            <ul>
                {{#each burgers}} {{#unless this.devoured}}
                <li>

                    {{this.burger_name}}

                    <form action="/{{this.id}}" method="POST">
                        <input type="hidden" name="devoured" value="false"> 
                        <button class="btn-primary" type="submit">Devour Burger!!</button> 
                    </form>
                </li>
                {{/unless}} {{/each}}

            </ul>
        </div>

        <div class="col-xs-12 col-md-4">
            <h2 class="text-center">ADD A BURGER</h2>
            {{!-- <form  action="burgers/create" method="POST"></form> --}}

            {{!--
            <div class="form-group">
                <label form="ca">Burger Name: </label>
                <input type="text" id="ca" name="name">
            </div> --}}

            <form class="create-update-form" action="burgers/create" method="POST">

                <div class="form-group">
                    <label form="ca">Burger Name: </label>
                    <input type="text" id="ca" name="name">
                </div>
                <button class="btn-primary" type="submit">ADD BURGER</button>

            </form>

        </div>
        <div class="col-xs-12 col-md-4">

            <h2 class="text-center">Devoured Burgers!!!</h2>
           {{!--  <form class="create-update-form" action="/{{this.id}}?" method="POST">
                <input type="hidden" name="devoured" value="true">
                <button class="btn-primary" type="submit">Devour Burger!!</button>
 --}}
                <ul>

                    {{#each burgers}} {{#if this.devoured}}

                    <li>
                        {{this.burger_name}}
                        {{!-- <form class="create-update-form" action="/{{this.id}}?" method="POST">
                            <input type="hidden" name="devoured" value="true">
                            <button class="btn-primary" type="submit">Devour Burger!!</button> --}}



                    </li>
                    {{/if}} {{/each}}

                </ul>
        </div>
    </div>

</div>